---
layout: home.njk
title: "Workbook 5: Additional UEIs"
meta:
  name: "Workbook 5: Additional UEIs"
  description: "Follow these step-by-step instructions for completing Workbook 5: Additional UEIs."
workbook:
  name: "additional-ueis-workbook"
---


{%  macro getInstructions(inst, named_range) %}
  {% for inst_nr, obj in inst %}
    {% if inst_nr == named_range %}
      {{ obj.instructions }}
    {% endif %}
  {% endfor %}
{% endmacro %}

{%  macro instructions(inst, wb) %}
  {% set the_sheet = "" %}
  {% for named_range, v in inst %}
    {% for sheet in workbooks[wb]["sheets"] %}
      {% if loop.index == 2 %}
        {% set the_sheet = sheet %}
      {% endif %}
    {% endfor %}
  {% endfor %}

  {% set ranges = the_sheet.open_ranges %}

  <dl>
  {% for column in ranges | sort(false, true, 'title_cell') %}
      <dt class="text-bold">Column {{column.title_cell.slice(0,1)}}: {{column.title}}</dt>
      <dd>
        {{ getInstructions(inst, column.range_name) }}
      </dd>
  {% endfor%}
  </dl>

{% endmacro %}

{% macro img(wb) %}
  <img src="{{config.baseUrl}}assets/img/workbooks/{{wb}}.png" 
    width=400 
    style="margin-left: 2em; margin-bottom: 2em; float: right; border: 1px solid #555;"/>
{% endmacro %}

<h2>Workbook 5: Additional UEIs</h2>

<div style="float: right">
    {{ img(workbook.name) }}
</div>
<div>
  <p>This workbook is only necessary if the single audit report covers multiple UEIs.</p>

  <h3>If the report covers multiple UEIs:</h3>

  <p><bold>Enter the UIEs one per row in the workbook.</bold></p>
</div>
  
<div>
  {{ instructions(workbooks.InstructionsAdditionalUEIs, workbook.name) }}
</div>
