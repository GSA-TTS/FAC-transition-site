---
permalink: "/feeds/atom/updates.xml"
eleventyExcludeFromCollections: true
metadata:
  title: "Updates from the FAC"
  description: "Periodic updates from the Federal Audit Clearinghouse"
  language: en
  base: "https://fac.gov/updates"
  author:
    name: "Clearinghouse staff"
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ metadata.language or page.lang }}">
  <title>{{ metadata.title }}</title>
  <subtitle>{{ metadata.description }}</subtitle>
  <link href="{{ permalink | htmlBaseUrl(baseUrl) }}" rel="self" />
  <link href="{{ baseUrl | addPathPrefixToFullUrl }}" />
  <updated>{{ collections.posts | getNewestCollectionItemDate | dateToRfc3339 }}</updated>
  <id>{{ baseUrl | addPathPrefixToFullUrl }}</id>
  <author>
    <name>{{ metadata.author.name }}</name>
  </author>
  {%- for post in collections.updates | reverse %}
    {%- set absolutePostUrl %}{{ post.url | htmlBaseUrl(baseUrl) }}{% endset %}
    <entry>
      <title>{{ post.data.title }}</title>
      <link href="{{ absolutePostUrl }}" />
      <updated>{{ post.date | dateToRfc3339 }}</updated>
      <id>{{ absolutePostUrl }}</id>
      <content type="html">{{ post.content | renderTransforms(post.data.page, baseUrl) }}</content>
    </entry>
  {%- endfor %}
</feed>